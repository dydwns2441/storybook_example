import React from "react";
import Button from "../common/Button";

const Header = () => {
  return (
    <>
      <header>
        <nav>
            버튼튼튼
        </nav>
      </header>
    </>
  );
};

export default Header;
